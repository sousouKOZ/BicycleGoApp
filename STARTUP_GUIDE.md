# BicycleGo 起動手順ガイド (AIレコメンド統合版)

このドキュメントは、BicycleGoアプリ（Flutter + Supabase + Python AIレコメンド）をクリーンな状態から立ち上げて、ローカル環境でデモを実行するための完全な手順書です。

---

## 🚀 最短起動ステップ

すべての環境（Docker, Supabase, Python, Flutter）がインストール済みであることを前提とします。
ターミナルを開き、プロジェクトのルートディレクトリ（`BicycleGoApp`）で以下の手順を順番に実行してください。

### Step 1: データベースの起動と初期化
まず、Supabaseのローカル環境を立ち上げ、データベースを真っ新な状態にリセットします。

```bash
# 1. Supabaseを起動（すでに起動している場合は不要）
supabase start

# 2. データベースをクリーンにリセットし、テーブルを作成
supabase db reset
```

### Step 2: シードデータ＆本番データのロード (重要)
次に、AIが計算に用いる「11万件の過去履歴（Seed）」と、アプリ上に表示する「30件の提携店舗（Stores）」をデータベースに流し込みます。
**※この処理を行わないと、マップに店舗が表示されず、AIのレコメンドも機能しません。必ず `db reset` の直後に実行してください。**

```bash
python load_data.py
```
※ 完了すると `Phase 2 complete! All data successfully loaded and calculated.` と表示されます。文字化け対策として、本番用の30店舗には自動的に「提携カフェ 1号店」等の綺麗な日本語名が割り当てられます。

### Step 3: AIレコメンドサーバーの起動
Pythonを用いてAIレコメンドAPI（ポート5001番）を立ち上げます。
**このターミナルは閉じずに、起動したままにしてください。**

```bash
python recommendation_api.py
```
※ `* Running on http://127.0.0.1:5001` と表示されれば準備完了です。

### Step 4: Flutterアプリの起動
最後に、別タブのターミナル（またはVSCodeのデバッグ機能）を使ってFlutterアプリを立ち上げます。デモ用の設定フラグを必ず付与してください。

```bash
flutter run --dart-define-from-file=env/dev.json --dart-define=DEMO=true
```

---

## 💡 システムの構成と挙動について

*   **UI（画面表示）**: マップ上の駐輪場をタップして詳細シートを引き上げると、AIが算出した「あなたへのおすすめの理由（例：📝 似た嗜好のユーザーがよく利用しています）」とともに、提携店舗トップ3が表示されます。
*   **バックグラウンド（クーポン自動発行）**: アプリ上で駐輪（NFCスキャン等）を行い15分が経過すると、裏側で `issue_coupons` (Edge Function) が自動実行されます。この時もPython AIサーバーへ問い合わせが行われ、**「最もスコアが高い1店舗」のクーポンが実際に発行**されます。
*   **コールドスタート対策**: 本番データ（couponsテーブルの実績）が全くない初期状態でも、AIは「過去の11万件のチェックイン履歴（seed）」と「店舗カテゴリの親和性」を組み合わせて、的確にレコメンドを算出します。

## 🔧 トラブルシューティング

**Q. マップに提携店舗（特典アイコン）が表示されない**
A. `load_data.py` の実行を忘れている可能性があります。Step 2 を再度実行してください。

**Q. 詳細シートでおすすめ店舗が出ない・エラーになる**
A. Pythonサーバー（Step 3）が立ち上がっていない、またはポート番号が `5001` ではない可能性があります。起動状況を確認してください。

**Q. 提携店舗の名前や特典内容をカスタマイズしたい**
A. `load_data.py` の実行後、ブラウザで Supabase Studio (http://127.0.0.1:54323) を開き、`stores` テーブルのデータを直接編集することで、アプリに即座に反映させることができます。
